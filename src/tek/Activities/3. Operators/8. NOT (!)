8. NOT (!):
You are building an application where certain features are available only to non-admin users. Given boolean isAdmin = true;, write a condition to check if the current user is NOT an admin and hence can access those features.