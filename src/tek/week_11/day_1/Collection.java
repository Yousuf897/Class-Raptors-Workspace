package tek.week_11.day_1;

public class Collection {

    /*
    * Real-life Analogy:

Think of a library. It's a place where you can store, organize, and retrieve books. The library offers different sections and
* methods to place your books, like on shelves, in sections by genre, or in special collections for rare editions. You can add new
* books, remove old ones, or go through each shelf to find a specific title.

In programming, especially in Java, a collection works similarly to a library. It's a framework that provides an architecture to
* store and manipulate a group of objects. Just like a library can have different sections, Java's collection framework includes
* various types of collections like lists, sets, and queues, each with its own way of storing and accessing elements.
*
Explanation in Programming:

Java Collections are a set of classes and interfaces that implement commonly reusable collection data structures. Essentially,
* they are data structures that can hold multiple items of the same type – they are built-in to Java’s java.util package.
* Collections can dynamically grow and shrink in size.

Key Characteristics of Java Collections:

    Dynamic Operations: Collections can grow and shrink in size dynamically, unlike arrays which have a fixed size.

    Element Manipulation: You can add, remove, and search for elements in a collection.

    Type Safety: Generics in Java allow collections to specify the type of objects they can contain, providing compile-time type safety.

    Ready-made Methods: Collections come with powerful, ready-to-use methods for sorting, searching, and modifying data.

    Iterability: Collections can be iterated over using iterators or enhanced for loops.

    Major Interfaces of the Collection Framework:

    Collection Interface: The root interface of the collection hierarchy. A collection represents a group of objects known
    * as its elements.

    List Interface: An ordered collection (also known as a sequence) that can contain duplicate elements.

    Set Interface: A collection that cannot contain duplicate elements.

    Queue Interface: A collection used to hold multiple elements prior to processing, typically in order but not necessarily so.

    Map Interface: Although not a true collection (and therefore doesn't inherit from the Collection interface), it represents
    * a collection-like entity that maps keys to values and cannot contain duplicate keys.
    *
    * */

}
